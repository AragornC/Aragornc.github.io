# 个人页重设计施工单（Claude → Codex）

你是施工方，我（Claude）定架构。这份 brief 是合同。**先读完整份再动手，开工前在终端打印你的执行计划（5 行以内），等我（用户/我）回 ok 再写文件**。

## 背景
- 项目路径：`/Users/aragorn/Desktop/Aragornc.github.io`
- 当前是一个 GitHub Pages 静态站点，纯 HTML + 内联 CSS，无构建工具
- 本地预览：`http://localhost:8000/`（已经在跑，由 Claude 启动的 python http.server）
- 内容很好不要改，**只动样式/结构/IA**。具体不改 `topics/agent-knowledge/*.html` 等长文正文，只动它们的外壳/导航/排版样式

## 用户的不满
1. 整站没有"个人博客"的感觉，更像 dashboard
2. UI 不美观：胶囊小标签（`.meta span` 浅蓝椭圆）显得廉价
3. 信息架构没逻辑：hero/卡片/map/status 四种视觉语言堆叠

## 设计方向（已定）
**参考站**：
- lilianweng.github.io（清爽知识库 + 文章列表）
- eugeneyan.com（taxonomy + writing list）
- maggieappleton.com（视觉 IA，但别学过头）
- karpathy.ai（极简）

**目标气质**：editorial、知识库、个人色彩、克制。
- 不是 SaaS 落地页（去掉 hero 大图横铺）
- 不是 dashboard（去掉那些胶囊 meta 标签）
- 是"作者的知识基地"（强调身份 + 写作列表 + 主题分类）

## IA（三层 surface）

### 1. Home `/index.html`
```
┌──────────────────────────────────────────────┐
│ Logo/Name (AragornC)        nav: 专题 / About│ ← 顶部窄 nav
├──────────────────────────────────────────────┤
│                                              │
│  你好，我是 AragornC。                        │ ← 大标题 identity
│  在快手做 AI native 风控；                    │
│  这里是我关于 AI agent 的笔记和长文。          │
│                                              │
│  GitHub · Email · RSS（小字 inline 链接）     │
│                                              │
├──── Now ───────────────────────────────── ──┤
│  当前在写：…（一段话，可手改）                  │
├──── Writing ─────────────────────────────────┤
│                                              │
│  [封面16:9 全宽]                              │ ← 专题卡，封面在顶
│  Agent 知识包：3 层架构 + LangChain 演进       │
│  12 章 · 50 图 · 2026-05 更新                 │ ← 元信息纯文本
│  一段 2-3 行的 abstract……                     │
│                                              │
│  [封面16:9 全宽]                              │
│  AI Agent Orchestration：从 Transformer 到… │
│  8 章 · 2026-04 更新                         │
│  ……                                          │
│                                              │
│  [封面16:9 全宽]                              │
│  AI Native 风控：业务线立项论证                │
│  ……                                          │
│                                              │
├──── Topics（轻量索引） ───────────────────────┤
│  模型与训练 · Agent 模式 · 工程 Harness · 协议│ ← 一行 tag 链接
├──── Footer ──────────────────────────────────┤
│  © 2026 AragornC · github 链接                │
└──────────────────────────────────────────────┘
```

### 2. Topic Index `/topics/<slug>/index.html`
（现有 3 个：agent-knowledge / agent-orchestration / ai-native-risk-case）
```
返回首页 ← 顶部窄链接
专题标题（大）
abstract 1-2 段
"你将读到什么" 3-5 条 bullet

章节列表（如有多篇）：
- 章节标题
  一句话描述 · 阅读时长 · 更新日期
- 章节标题
  ……
```
- `agent-knowledge` 有 12 篇子文章，需要列出
- `agent-orchestration` / `ai-native-risk-case` 各 1 个 index（结构暂保留，套新样式）

### 3. Article（先不动 12 篇正文样式，只确保它们能 inherit 全局 css 不冲突）

## 视觉系统（design tokens）

```css
:root {
  --bg: #fafaf7;          /* 米白 */
  --paper: #ffffff;
  --ink: #1a1a1a;          /* 接近黑，不纯黑 */
  --ink-2: #4a4a4a;        /* 次要文本 */
  --muted: #888;           /* 元信息 */
  --line: #e8e6df;         /* 极淡分割线 */
  --accent: #245f73;       /* 保留原 accent */
  --accent-hover: #153f4d;
  
  --font-sans: "Inter", -apple-system, "PingFang SC", "Hiragino Sans GB", sans-serif;
  --font-serif: "Source Serif Pro", "Noto Serif SC", Georgia, serif;
  
  --maxw-prose: 680px;     /* 正文最大宽度 */
  --maxw-page: 880px;      /* 整页最大宽度，比当前 1180 窄，更"博客" */
}
```

### 排版
- Body：16px / line-height 1.75
- H1 site title：32px sans-serif，不要超大
- 专题卡标题：22px sans-serif bold
- 不用大写英文 kicker（删除 `.kicker` 那种全大写小字）
- Meta：14px `var(--muted)`，纯文本用 `·` 分隔，**不要胶囊背景**

### 间距
- 区块间距：80px
- 卡片间距：48px

### 颜色规则
- 全站只允许：bg / paper / ink / ink-2 / muted / line / accent
- **禁用**：浅蓝胶囊背景 `#eef6f8`、黄底 `#fff6e8`、暖棕 `#8a4e27`、`#fbf2dc` 渐变那种
- status 黄底块直接删掉，用纯文本 + 小字斜体放底部即可

### 卡片
- 不要 `box-shadow: 0 18px 44px`（太重）
- 用 `border: 1px solid var(--line)` 即可，hover 时 border 变深
- 圆角：6px（不要 10px+）

### Cover 图
- 16:9，放卡片顶部全宽
- 不要左右分栏
- 第三个卡（ai-native-risk-case）那个 SVG 三角形不合适做封面 → 改成纯文字卡（无图也行），或者换张更合适的图

## 文件产出

1. **新建** `assets/css/site.css`：所有共享样式（tokens + base + components）
2. **重写** `index.html`：
   - 移除所有内联 `<style>`
   - 链接 `assets/css/site.css`
   - 按上面 IA 重组结构
3. **改造** 3 个 `topics/*/index.html`：
   - 都链接 `assets/css/site.css`
   - 套统一头/导航/footer
   - 移除冗余内联样式
4. **不动** `topics/agent-knowledge/*.html`（12 篇文章）—— 它们各自有完整页面，本期不重构
5. 可选：在 `assets/css/` 加 `article.css` 给文章页留口子，但本期不实施

## 关于身份信息
身份卡内容用以下文案（如不合理可微调）：
- 名字：AragornC
- 一句话：在快手做 AI native 风控；这里是关于 AI agent 的笔记和长文。
- 链接：GitHub `https://github.com/aragornc` · Email `zc427@cornell.edu`

## 验收标准
- 桌面（>1024px）打开 `http://localhost:8000/` 看起来像一个"作者的个人博客"，不像 SaaS 落地页
- 没有任何浅色胶囊背景的小标签
- 三个专题卡视觉一致（封面 16:9 在顶 → 标题 → meta → abstract）
- 移动端（<560px）正常堆叠
- 三个 topic index 页风格与首页一致

## 工作流
1. 看完这份 brief
2. 在终端打印你的施工计划（5 行内：文件清单 + 改动要点）
3. 等我（Claude，会通过 cmux 给你回复）回 "ok 干"
4. 开干，每完成一个文件 print 一行进度
5. 全部完成后说"完事"

不要：擅自改 12 篇 agent-knowledge 长文正文、不要引入构建工具（npm/vite 都不要）、不要引入图标库或 web font CDN（除非纯 CSS fallback 兜底，离线也要能看）、不要写 JS（除非必要，比如 mobile nav toggle，本期可以不要）。

开始吧。
